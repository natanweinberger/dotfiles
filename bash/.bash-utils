# Define the command patterns that won't be saved to bash history
export HISTIGNORE="&:[ ]*:exit"

# Turn off line wrapping in mycli
# https://www.mycli.net/pager
# -R fixes git color formatting
export LESS="-SXR"

# Find external IP address via DNS
# alias whatsmyip_dns="dig @resolver1.opendns.com ANY myip.opendns.com +short"

# Find external IP address via HTTP
alias whatsmyip="curl -s http://whatismyip.akamai.com/"
